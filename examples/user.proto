syntax = "proto3";

message User {
    string name = 1;
    int32 age = 2;
}

message UserRequest {
    int32 user_id = 1;
}

message UserResponse {
    string name = 1;
    int32 age = 2;
}

service UserService {
    rpc GetUser(UserRequest) returns (UserResponse);
}
